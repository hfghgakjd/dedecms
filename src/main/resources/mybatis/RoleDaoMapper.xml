<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.snowfog.dao.RoleDao">
    <select id="findRoleByRoleId" resultType="com.snowfog.entity.Role">
        select * from t_system_role t where t.id = #{roleId}
	</select>

    <select id="findRoleTypeByUrl" resultType="java.util.HashMap">
		select t.* from t_system_role t left JOIN t_system_menu_role t1 on t1.role_id = t.id
		left join t_system_menu t2 on t2.id = t1.menu_id
		where t2.url = #{url};
	</select>
</mapper>